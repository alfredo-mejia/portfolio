import { NAV_CARDS } from "../constants/navigation";

function Resume() {
  return (
    <section className="content-center">
      <header>
        <h1>{NAV_CARDS.RESUME.TITLE}</h1>
      </header>

      <div></div>

      <footer></footer>
    </section>
  );
}

export default Resume;
