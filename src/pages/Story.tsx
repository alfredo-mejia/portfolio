import { NAV_CARDS } from "../constants/navigation";

function Story() {
  return (
    <section className="content-center">
      <header>
        <h1>{NAV_CARDS.STORY.TITLE}</h1>
      </header>

      <div></div>

      <footer></footer>
    </section>
  );
}

export default Story;
