import { NAV_CARDS } from "../constants/navigation";

function Projects() {
  return (
    <section className="content-center">
      <header>
        <h1>{NAV_CARDS.PROJECTS.TITLE}</h1>
      </header>

      <div></div>

      <footer></footer>
    </section>
  );
}

export default Projects;
