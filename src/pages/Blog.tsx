import { NAV_CARDS } from "../constants/navigation";

function Blog() {
  return (
    <section className="content-center">
      <header>
        <h1>{NAV_CARDS.BLOG.TITLE}</h1>
      </header>

      <div></div>

      <footer></footer>
    </section>
  );
}

export default Blog;
